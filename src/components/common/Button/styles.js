import { StyleSheet } from 'react-native'

export default StyleSheet.create({

  wrapper: {
    height: 40,
    borderRadius: 4,
    marginVertical: 5,
    paddingHorizontal: 5,
    alignItems: "center",
    justifyContent: "space-evenly"
  },
  loaderSection: {
    flexDirection: "row"
  }

})